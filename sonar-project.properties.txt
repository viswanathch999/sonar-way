# Required metadata
sonar.projectKey=sonar-way
sonar.projectName= sonar-way
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Encoding of the source files
sonar.sourceEncoding=UTF-8
